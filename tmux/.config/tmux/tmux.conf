set-option -g default-terminal "${TERM}"
set-option -g mouse "on"

set-option -ga terminal-overrides ",*256*:Tc"
# set-option -ga terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
# set-option -ga terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

bind-key -n Home send Escape "OH"
bind-key -n End send Escape "OF"

# set-option -g message-command-style bg=black,fg=colour230
# set-option -g message-style bg=colour230,fg=default
# set-option -g mode-style bg=colour230,fg=default

# set-window-option -g window-status-current-style fg=colour0,bg=colour6
# set-window-option -g window-status-style fg=colour232,bg=default

bind-key * set-window-option synchronize-pane
bind-key r source-file ~/.tmux.conf

set-window-option -g mode-keys vi
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

set -g @theme-status-interval 30
set -g @theme-status-bg default
set -g @theme-status-left-bg default
set -g @theme-status-left-fg cyan
set -g @theme-status-right-bg default
set -g @theme-window-status-current-fg black
set -g @theme-window-status-activity-fg colour15
set -g @theme-window-status-activity-bg colour250
set -g @theme-window-status-current-fg black
set -g @theme-window-status-current-bg blue
set -g @theme-mode-bg cyan
# set @theme-mode-fg black
# set -u @theme-status-interval
# set -u @theme-status-bg
# set -u @theme-status-left-bg
# set -u @theme-status-left-fg
# set -u @theme-status-right-bg
# set -u @theme-window-status-current-fg
# set -u @theme-window-status-activity-fg
# set -u @theme-window-status-activity-bg
# set -u @theme-window-status-current-fg
# set -u @theme-window-status-current-bg
# set -u @theme-mode-bg
# set -u @theme-mode-fg

source-file ~/.config/tmux/themes/basic.tmuxtheme
